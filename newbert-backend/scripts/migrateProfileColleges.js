require("dotenv").config(); const connectDatabase = require("../config/db"); const Profile = require("../Models/Profile"); const { resolveProfileCollege } = require("../services/collegeService");
async function run() { await connectDatabase(); const profiles = await Profile.find({ college: { $type: "string", $ne: "" }, $or: [{ collegeId: null }, { collegeId: { $exists: false } }] }).lean(); let matched = 0; for (const profile of profiles) if (await resolveProfileCollege(profile, { persist: true })) matched += 1; console.log(`Profiles scanned: ${profiles.length}`); console.log(`Matched: ${matched}`); console.log(`Unresolved: ${profiles.length - matched}`); process.exit(0); }
run().catch((error) => { console.error(`College migration failed: ${error.message}`); process.exit(1); });
