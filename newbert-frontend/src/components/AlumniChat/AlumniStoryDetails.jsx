import {AnswerValue} from './AnswerFields';
import '../../alumni-chat.css';
const sections=[['careerOutcome','Career outcome'],['placement','Selection story'],['gateProfile','GATE journey'],['psuProfile','PSU selection'],['dsaPreparation','DSA at selection'],['skillsAtSelection','Skills at selection'],['projects','Projects'],['internships','Internships'],['interviews','Interview rounds'],['preparation','Preparation'],['preparationJourney','Preparation timeline'],['resources','Recommended resources'],['advice','Advice for juniors'],['adviceDetails','More lessons for juniors'],['mentorship','Mentorship preferences']];
export default function AlumniStoryDetails({story,verified}){const profiles=Object.entries(story).filter(([key])=>key.startsWith('practice:')).flatMap(([,value])=>Array.isArray(value)?value:[value]);return <div className="ac-public-story space-y-6"><p className="text-xs font-semibold text-slate-500">{verified?'Verified outcome':'Self-reported story · not independently verified'}</p>{(profiles.length>0||story.socialLinks)&&<section className="surface p-6"><h2 className="text-lg font-extrabold text-slate-950">Professional & practice profiles</h2><div className="mt-4 flex flex-wrap gap-4">{profiles.filter(p=>p.profileUrl).map((p,i)=><a key={i} href={p.profileUrl} target="_blank" rel="noreferrer" className="text-sm font-bold text-orange-600">{p.platformLabel||p.platform} ↗</a>)}{Object.entries(story.socialLinks||{}).filter(([,url])=>url).map(([key,url])=><a key={key} href={url} target="_blank" rel="noreferrer" className="text-sm font-bold capitalize text-orange-600">{key} ↗</a>)}</div></section>}{sections.filter(([key])=>story[key]&&(Array.isArray(story[key])?story[key].length:Object.keys(story[key]).length)).map(([key,title])=><section className="surface p-6" key={key}><h2 className="text-lg font-extrabold text-slate-950">{title}</h2><div className="mt-4 whitespace-pre-wrap text-sm leading-7 text-slate-600"><AnswerValue value={story[key]}/></div></section>)}</div>;}
