import { useEffect, useState } from "react"; import API from "../Services/api";
const EMPTY_REQUEST = { name: "", university: "", city: "", state: "Uttar Pradesh", course: "B.Tech", website: "" };
export default function CollegeAutocomplete({ value, collegeId, onChange, onSelect }) { const [suggestions, setSuggestions] = useState([]); const [open, setOpen] = useState(false); const [requestOpen, setRequestOpen] = useState(false); const [request, setRequest] = useState(EMPTY_REQUEST); const [message, setMessage] = useState(""); const [activeIndex, setActiveIndex] = useState(-1); useEffect(() => { let active = true; const query = String(value || "").trim(); if (query.length < 2 || collegeId) { setSuggestions([]); return undefined; } const timer = setTimeout(() => API.get("/colleges/search", { params: { q: query } }).then(({ data }) => { if (active) { setSuggestions(data.colleges || []); setOpen(true); setActiveIndex(-1); } }).catch(() => { if (active) setSuggestions([]); }), 300); return () => { active = false; clearTimeout(timer); }; }, [value, collegeId]); const choose = (college) => { onSelect(college); setSuggestions([]); setOpen(false); }; const keyDown = (event) => { if (!open || !suggestions.length) return; if (event.key === "ArrowDown") { event.preventDefault(); setActiveIndex((index) => Math.min(index + 1, suggestions.length - 1)); } else if (event.key === "ArrowUp") { event.preventDefault(); setActiveIndex((index) => Math.max(index - 1, 0)); } else if (event.key === "Enter" && activeIndex >= 0) { event.preventDefault(); choose(suggestions[activeIndex]); } else if (event.key === "Escape") setOpen(false); }; const submitRequest = async (event) => { event.preventDefault(); setMessage(""); try { const { data } = await API.post("/colleges/request", request); setMessage(data.message); setRequest(EMPTY_REQUEST); } catch (error) { setMessage(error.response?.data?.message || "Unable to submit college request."); } }; return <><label className="relative text-sm font-bold text-slate-800">College *<input value={value} onKeyDown={keyDown} onChange={(event) => { onChange(event.target.value); setOpen(true); }} placeholder="Type your college" className="control mt-2 w-full rounded-md border border-slate-300 p-2 text-sm text-slate-900 focus:border-orange-500 focus:outline-none"/>{collegeId ? <span className="mt-1 block text-xs font-semibold text-emerald-700">Verified college selected</span> : <span className="mt-1 block text-xs text-slate-500">Select a verified college from the suggestions.</span>}{open && suggestions.length ? <div className="absolute z-30 mt-1 max-h-72 w-full overflow-y-auto rounded-md border border-slate-200 bg-white shadow-xl">{suggestions.map((college, index) => <button type="button" key={college.collegeId} onMouseDown={(event) => event.preventDefault()} onClick={() => choose(college)} className={`block w-full border-b border-slate-100 px-3 py-3 text-left ${index === activeIndex ? "bg-orange-50" : "hover:bg-orange-50"}`}><span className="block text-sm font-bold text-slate-900">{college.name}</span><span className="block text-xs font-semibold text-slate-600">{college.shortName}</span><span className="text-xs text-slate-500">{college.university} · {college.city} · {(college.courses || []).join(", ")}</span></button>)}</div> : null}<button type="button" onClick={() => { setRequest((current) => ({ ...current, name: value || current.name })); setRequestOpen(true); }} className="mt-2 text-xs font-extrabold text-orange-700">Can't find your college? Request to add college</button></label>{requestOpen && <div className="fixed inset-0 z-[80] grid place-items-center bg-slate-950/60 p-5"><form onSubmit={submitRequest} className="w-full max-w-lg rounded-xl bg-white p-6 text-slate-900"><div className="flex justify-between gap-4"><div><p className="text-xs font-extrabold uppercase tracking-widest text-orange-600">College request</p><h2 className="mt-2 text-xl font-black">Request to add your college</h2></div><button type="button" onClick={() => setRequestOpen(false)} className="text-sm font-bold">Close</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[["name","College name *"],["university","University"],["city","City"],["state","State"],["website","College website"]].map(([key,label]) => <label key={key} className="text-sm font-bold">{label}<input required={key === "name"} type={key === "website" ? "url" : "text"} value={request[key]} onChange={(event) => setRequest({ ...request, [key]: event.target.value })} className="mt-1 w-full rounded-md border border-slate-300 p-2"/></label>)}<label className="text-sm font-bold">Course<select value={request.course} onChange={(event) => setRequest({ ...request, course: event.target.value })} className="mt-1 w-full rounded-md border border-slate-300 p-2">{["B.Tech","M.Tech","BCA","MCA","Other"].map((item) => <option key={item}>{item}</option>)}</select></label></div>{message && <p className="mt-4 text-sm font-semibold text-orange-700">{message}</p>}<button className="mt-5 rounded-lg bg-orange-500 px-4 py-3 text-sm font-extrabold text-slate-950">Submit request</button></form></div>}</>; }
